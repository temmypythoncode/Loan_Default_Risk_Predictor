# Data (real datasets shouldn't go in git — Kaggle terms + repo size)
data/loan_data.csv

# Generated outputs — models and intermediate data are large/regenerable,
# but keep the evaluation plots as evidence of results
outputs/*.pkl
outputs/*.csv

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
